name: Fuzz Smoke

on:
  schedule:
    - cron: "30 3 * * 0"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  checks:
    uses: esaueng/remus/.github/workflows/fleet-fuzz.yml@6d8b64129c5d77748f089451450452d277892b37
    permissions:
      contents: read
      pull-requests: read
