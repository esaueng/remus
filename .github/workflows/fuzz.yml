name: Fuzz Smoke

on:
  schedule:
    - cron: "30 3 * * 0"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  checks:
    uses: esaueng/remus/.github/workflows/fleet-fuzz.yml@de062d7c06c85ee5cf524aca019d8a73485e61e8
    permissions:
      contents: read
      pull-requests: read
