name: Fuzz Smoke

on:
  schedule:
    - cron: "30 3 * * 0"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  checks:
    uses: esaueng/remus/.github/workflows/fleet-fuzz.yml@f7269dfc454fba99bb3a908fb43a8873559135bd
    permissions:
      contents: read
      pull-requests: read
