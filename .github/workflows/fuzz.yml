name: Fuzz Smoke

on:
  schedule:
    - cron: "30 3 * * 0"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  checks:
    uses: esaueng/remus/.github/workflows/fleet-fuzz.yml@055b7fc0e2f8916e0615760e7d800352a630fbbd
    permissions:
      contents: read
      pull-requests: read
