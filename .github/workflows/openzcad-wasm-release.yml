name: Build OpenZCAD WASM Candidate

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  checks:
    uses: esaueng/remus/.github/workflows/fleet-wasm-candidate.yml@dbb227610933985440f18eb38adb24323cb6c391
    permissions:
      contents: read
      pull-requests: read
