name: Build OpenZCAD WASM Candidate

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  checks:
    uses: esaueng/remus/.github/workflows/fleet-wasm-candidate.yml@32257393a541388000a658e9893a020824fc4de9
    permissions:
      contents: read
      pull-requests: read
