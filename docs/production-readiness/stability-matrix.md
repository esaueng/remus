# Stability matrix

The README labels below are retained; no feature is promoted by this audit.
Rows marked **blocked** lack the full production evidence for their advertised
domain. The P0/P1 defects found by this audit are closed and the fork CI gate
passes; rows can remain blocked on broader domain matrices.

| README category | Feature | Current label | Disposition |
| --- | --- | --- | --- |
| Primitives | Box, cylinder, cone, sphere, torus, ellipsoid | Stable | Blocked: native/WASM invalid-input, scale, and full postcondition matrix incomplete. |
| Primitives | Convex hull, Minkowski sum | Stable | Blocked: degenerate/property coverage incomplete. |
| Booleans | Plane/cylinder/cone/sphere/NURBS union, cut, intersect | Stable | Guarded: cavity semantics pass; mesh fallback is bounded, deterministic, and fail-closed; the active 64-cut release test and final fork CI pass. A cylindrical face crossing a planar face of the other operand (a boss overhanging a plate edge, a bore flush with a wall) is analytic and exact across the overlap sweep, and an acceptance gate rejects a result that has silently lost an operand. Coaxial blind-bore cuts retain the canonical unsplit cylinder seam, exact volume, and watertight indexed tessellation across wall/radius 0.01–0.10 and 1e-3–1e3 scale (`regress_thin_wall_coaxial_bore.rs`). The tangent-boss d/r +0.01/0/-0.01 witnesses retain closed-form material across 1e-3/1/1e3 scale: tangent is exact at 1e-3 and unit scale; at 1e3, `ExactOnly` refuses and the permissive path discloses its approximation budget. Cross-drilled shafts are qualified against orthogonal-cylinder volume oracles and non-empty closed/manifold display meshes for three bore ratios at scales 0.1/1/10. General-position equal-radius sphere×sphere fuse/cut/intersect retain four analytic sphere faces and match independent lens/inclusion–exclusion volumes plus closed manifold meshes across three deflections, including an oblique-center witness. In-review PR #218 adds exact cellular two-solid output: a severing planar cut returns two independently valid 400-volume Compound members and disjoint planar fuse returns exact separate members, with deterministic total per-region construction lineage and direct/batch WASM parity. PR #219 qualifies pairwise-disjoint Compound operands for member-preserving fuse, distributed exact intersect, and distributed single-tool cut; intersecting-member fuse and multi-tool cut fail closed pending recursive lineage composition. Sliver-crossing fuses — roughly 1e-5 to 0.05 mm on r = 10 — still use disclosed approximation. Broader domain matrices remain pending. |
| Booleans | Batch fuse-all | Stable | Blocked: depends on boolean correctness/fallback contract. |
| Booleans | Torus booleans | Beta | Retained, evidence improved 2026-08-21: coaxial torus×cylinder and axis-centred torus×sphere sections are exact circles (`exact_torus_cylinder`/`exact_torus_sphere`; the quartic marcher's NURBS fits made these configurations effectively hang — now seconds), any-plane-through-centre cuts halve the torus exactly (a containment-shortcut unsoundness read the torus as contained from its seam vertex alone; fixed with surface-sample witnesses), and the coaxial-cylinder cut matches its closed form through the disclosed fallback (`qualify_torus_boolean.rs`). Still Beta: carving a closed torus face into tube bands has no splitter arm, so these configurations resolve approximately; the torus×sphere fuse is a typed bounded refusal (ready-repro pinned). |
| Modifiers | Fillet, chamfer | Stable / Experimental | Guarded: planar line-edge requests use validated manifold builders, and bare cylinder/cone rims use the exact validated toroidal fillet assembler. On a cylinder cap rim that assembler covers the whole radius range `0 < f < r_c` — including `f >= r_c/2`, where the carrier torus is a horn or spindle but the quarter-tube band cut from it is not, verified against the closed-form removed volume `pi((r_c - f) f^2 (2 - pi/2) + f^3/3)` and band area `pi^2 f (r_c - f) + 2 pi f^2` across the sweep. `f >= r_c` (the rolling ball no longer fits inside the cylinder), and the vertex-tolerance sliver below it (the cap face it leaves is not a face), are refused as typed `RadiusTooLarge` naming the edge and the limit — not as a partial result. The hemispherical `f = r_c` end is a different topology and is not emitted. The wall's own axial extent is a separate limit, reported the same way. A blind hole's FLOOR rim (the concave inward case) now covers the same `0 < f < r_c` range as the cap rim: the historical wrong-direction defect no longer reproduces (the r = 3 hole rounded at r = 1 adds its exact 3.746 collar, pinned against the closed form across the sweep including the horn/spindle carrier regimes in `qualify_blind_hole_floor_fillet.rs`), and `f >= r_c` is refused as typed `RadiusTooLarge`. V2 wrappers reject partial or invalid results and preserve cavity shells. Both engines now share G1 expansion and standard radius-law definitions. Every public entry point is fail-closed (2026-09-01): the legacy engines (`fillet`, `fillet_rolling_ball`, `fillet_variable`) and the flat-bevel chamfer are individually transactional and postcondition-validated — the historical silent-wrongness cells (an oversized variable fillet returning a volume-inflated solid as success, an invalid canal-surface solid returned as `Ok`, a foreign-edge selection answered with a clone of the input) are typed refusals now, and results that validate closed but regress orientation/manifold checks against the input baseline are refused rather than shipped (this surfaced two pre-existing damaged-success cases the old guards could not see: the blend-adjacent second-pass fillet and the gridfinity lip peak-rim fillet, both now honest typed refusals pending the B4 trimmer completion). Refusals carry the stable `blend_failure_code` vocabulary on every surface (message prefix on direct bindings, `kernelCode` on the structured batch contract). In-review PR #226 additionally qualifies the v2 straight-edge perpendicular-plane variable-radius walking band: standard-law whole-domain extrema, typed tolerance-collapse and caller-supplied local-limit refusals, an analytic ruled-surface plus closed-form linear volume oracle, and sampled S-curve radius/incidence/tangency invariants. Opaque custom callbacks are preserved and station-checked rather than endpoint-linearized, but arbitrary between-sample certification and trimmed-solid assembly remain unqualified. In-review PR #228 qualifies constant-radius closed curved-support assembly for coaxial cylinder/cone, cylinder/sphere, cone/cone, and a segmented orthogonal cylinder/cylinder cross-drilled rim. The native matrix pins validation, zero free/non-manifold edges, watertight welded meshes, and B-Rep/mesh volume agreement within 2%; direct and batch WASM agree. The existing closed-rim chamfer matrix remains green. In-review PR #231 qualifies same-radius planar N-way vertex blends with one common tangent ball and one connected material-side orientation. All-edge box and four-stripe pyramid witnesses pin analytic sphere caps, angular-tolerance G1 seams, closed/manifold B-Reps, watertight meshes, and B-Rep/mesh volume agreement; four- and five-stripe torture cases build, and direct/batch WASM agree. Transverse planar runouts retain strictly trimmed ellipse edges. Alternating convex/concave material sides refuse transactionally with `unsupported-vertex-blend`; nonplanar and variable-radius corners remain experimental. The legacy rolling-ball assembler remains until the walking engine reaches planar/corner assembly parity; open/non-coaxial curved assembly, curved-support chamfers, and variable-radius curved assembly remain experimental and fail closed. |
| Modifiers | Resize/remove analytic blend band | Experimental | Guarded: `resize_blend` re-derives constant-radius torus/cylinder band membership and two tangent supports from exact topology, checks the caller radius through kernel tolerance, snapshots the arena, and accepts only strictly valid solids with sane monotonic volume. Plane/plane bands and closed plane/cylinder rims grow, shrink, and remove exactly. In-review PR #228 flips the pinned cylinder-wall × cone-wall `unsupported-support-pair` boundary: removing a radius-0.25 band recovers the exact sharp shoulder with relative volume error below 1e-7, and positive reconstruction at radius 0.15 yields a validated, watertight solid with monotonic volume. An imported Shapr3D radius-4 band also rebuilds at radius 3 on the exact carrier-proven external tangent branch (reversed torus, major radius 19.5) rather than silently taking the ordinary material-side branch. Freeform contact, radius mismatch, oversized radii, ambiguous topology, invalid output, and partial fillet results refuse with stable codes and restore existing handle slots. Native and WASM bindings return construction-derived face evolution; variable-radius bands remain out of scope. |
| Modifiers | Shell | Stable | Guarded: offset-engine shell results require closed topology and L3 validation; broader curved and excluded-face matrices remain pending. |
| Modifiers | Offset, thicken, mirror, pattern | Stable | Guarded: offset no longer skips failed faces/walls, and validates closure and orientation on every shell, not just the outer one. A solid with cavity shells is now offset rather than refused — the shell partition is preserved and an outward distance shrinks each cavity — with the necessary shell-separation condition enforced and the input refused when it fails; excluding faces from such a solid still refuses. `JointType::Arc` now builds the rolling-ball offset of a convex polyhedron, checked against the Minkowski/Steiner closed form for volume and area and holding it from 1e3 to 1e-3 scale; a curved source face, a concave or tangent edge, a holed face, a cavity, an excluded face, or an inward distance each refuse with their own reason rather than fall back to the mitred joint. Still refusing, unchanged: global self-intersection removal, and NURBS-NURBS 3D intersection. |
| Modifiers | Push/pull face | Stable (declared domain) | Guarded: either untrimmed cap of a three-face analytic cylinder now rebuilds exactly for positive and negative distances, rotated and translated frames, and 1e-3/1e3 scale; inward top-cap edits no longer return the removed slab or faceted fallback. Closed-form volume, three-face topology, watertightness, collapse-boundary refusal, and deterministic WASM replay are pinned in `push_pull.rs` and `push-pull-cylinder-top-cap.json`. Decorated cylinders and general planar faces retain their validated existing paths; generalized curved re-limitation remains pending. |
| Modifiers | Draft | Stable (planar faces) | Promoted 2026-08-21: planar domain qualified — closed-form volume oracles (wedge, frustum, mid-neutral invariance, negative angle) at 1e-3/1/1e3 scale, untouched holes carried through, typed refusals both-sides tested (zero angle, holed target face, cavity solid, non-planar target, foreign face), native + WASM batch determinism (`qualify_draft.rs`, wasm `qualify_ops_tests`). Non-planar faces stay a typed refusal. |
| Sweeps | Extrude | Stable | Blocked: full degenerate/cavity matrix incomplete. |
| Sweeps | Revolve, sweep, loft, pipe | Stable | Blocked: topology and nonconvergence budgets incomplete. In-review PR #222 qualifies one bounded Wire-profile cell: a validated closed planar polygonal wire sweeps to a validation-gated solid without aliasing its input, and native/direct/batch WASM match the exact rectangular-prism volume. Open and non-planar Wire profiles refuse typed and transactionally; broader wire geometry and scale matrices remain incomplete. |
| Sweeps | Helical sweep | Stable | Blocked: termination/performance evidence incomplete. |
| Sweeps | Non-planar profiles | Stable (declared bounds) | Promoted 2026-08-21: n-sided (≥5-edge) non-planar hole-free section boundaries cap with Coons patches exact on the ring chords (translation-sweep volume oracle), and partial revolutions of non-planar POLYGONAL boundaries close with the same caps (half-of-full-revolution oracle). Declared typed refusals, both-sides tested: holes on a non-planar cap, curved-edge non-planar partial-revolve boundaries, miter-corner sweep non-planar profiles. |
| Construction | Coons fill, sew, untrim | Stable | Blocked: topology postconditions incomplete. |
| Sectioning | Cross-section, split/trim/imprint by plane/sheet/body | Stable (declared domains) | Plane split remains blocked on its cavity and degeneracy matrix. In-review PR #214 qualifies one cellular sheet domain: a connected single cylindrical face crossing a box yields an inside-then-outside Compound of two individually valid cells, matches the cylinder closed form, conserves the box volume, and is deterministic across native/direct/batch WASM. PR #215 adds planar sheet-by-solid keep-inside/keep-outside trim with exact 100/96 area oracles; PR #216 adds effective-normal one-way and strict mutual planar sheet trims, then trims six carrier sheets and sews a deterministic valid six-face solid matching `make_box` volume exactly. PR #217 adds transversal planar solid imprint: every target patch survives, volume is unchanged, split and section lineage is construction-derived with zero unresolved events, an anchored face resolves BoundMany, and direct/batch WASM agree. All return validation-gated bodies with typed transactional refusals. Other split carriers, curved and multi-face sheet pairs, curved or same-domain imprint, and broader matrices remain unqualified. |
| Measurement | Bounding box, area, center of mass | Stable | Evidence improved: inner-shell area, signed volume, and center regressions pass; cross-drilled volume matches independent orthogonal-cylinder oracles across three bore ratios and scales 0.1/1/10 through WASM batch replay. Planar faces bounded by lines, circles, and parabolas now use exact boundary moments, including circular holes, with native scale/deflection and direct+batch WASM oracles. In-review PRs #210/#212 qualify the bounded unit-scale sheet cells: trimmed-NURBS area, conservative bounding box, area-weighted center with an offset-hole oracle, planar batch parity, and typed sheet-volume/non-sheet-area refusals. PR #222 adds body-level Wire length with exact native/direct/batch perimeter parity; broader wire geometry and scale matrices remain incomplete. Broader curved-face/volume, curved-cavity, and scale matrices remain incomplete. |
| Measurement | Distance and classification | Stable | Evidence pending: all three cavity classifiers now pass inner-shell regressions; general tolerance/domain matrices remain incomplete. |
| Drawing | Hidden-line projection | Stable | Evidence pending: public error/performance matrix incomplete. |
| Geometry | NURBS evaluation and fitting | Stable | Evidence improved: bounded scheduled fuzzing exercises rational patch construction, finite point/derivative evaluation, and typed corrupt-input refusal against a committed corpus. Degree-nine direct/cached evaluation, imported invariant, fitting, and large-degree budget matrices remain incomplete. |
| Geometry | Analytic intersections | Stable | Evidence pending: tolerance/domain matrix incomplete. |
| Geometry | Surface-surface intersection | Stable | Guarded: NURBS SSI consumes caller marching/queue/segment/branch, coupled-Newton, and recursive seed-subdivision budgets, and cooperatively cancels through seed discovery, every Newton refinement, phase boundaries, and marcher/adaptive-step iterations with typed `operation_cancelled`. Scheduled NURBS fuzzing uses an independent horizontal-plane oracle for returned points, parameters, and fitted curves, including a clustered-section regression. Parameter-space budgets remain incomplete. |
| Geometry | Curve-curve intersection | Stable | Evidence pending: termination/property matrix incomplete. |
| Tessellation | Adaptive/CDT/analytic optimization | Stable | Evidence improved: any face failure aborts solid tessellation; malformed-face regression passes. Cross-drilled coarse/fine display meshes are non-empty, closed, and manifold across three bore ratios and scales 0.1/1/10; `meshQuality` now accepts the render angular tolerance, reports triangle count, and rejects empty meshes as non-watertight. In-review PR #210 adds deterministic open sheet meshing for a trimmed NURBS patch and preserves an intentional sub-deflection triangular trim; PR #213 carries the same patch through native/direct/batch STEP exchange. Broader sheet geometry/scale/parity and performance evidence remains pending. |
| Repair | Healing, sewing, validation | Stable | Blocked: permissive healing can mask invalid result semantics. |
| I/O | STEP | Stable | Guarded: shared byte/entity limits are enforced and configurable. CAx-IF v4.6 volume/area/centroid declarations round-trip per solid with opt-in typed diagnostics and transactional malformed-property refusal. Physical Loop/Coedge authority retains positioned pcurves and periodic winding per use; both cylinder-seam branches and the external 48-pcurve analytic fillet fixture are byte-identical across write/read/write, while malformed count/endpoint authority rolls back. In-review PR #213 adds first-class `SHELL_BASED_SURFACE_MODEL` exchange over open/closed shells, with a deterministic trimmed-NURBS native/direct/batch witness and typed transactional refusals. General imported plane/conic SameParameter combinations remain typed unsupported rather than falsely proved. AP242, attributes, non-solid validation properties, and broader corpus evidence remain pending. |
| I/O | STL, 3MF, OBJ, PLY, glTF | Stable | Guarded: byte/entity limits are enforced; 3MF separately bounds uncompressed XML. Broader round-trip/integrity evidence remains pending. |
| I/O | IGES | Experimental | Retained by decision (2026-08-21, stabilization plan C3 gate): STEP is the supported B-Rep exchange path; IGES stays a declared preview/lossy subset rather than growing toward Stable. |
| Sketching | DogLeg solver | Stable | Evidence pending: nonconvergence budget and degeneracy matrix incomplete. |
| Feature recognition | Holes, pockets, chamfers, fillets | Stable (declared set) | Promoted 2026-08-21 for the declared set: precision pinned (plain bodies yield nothing; FilletLike no longer claims planar faces), recall pinned on constructed ground truth (through/blind holes with diameter, all-planar rectangular pockets — previously undetectable, chamfers), deterministic ordered traversal (`qualify_feature_recognition.rs`). Outside the declared set, absence of a claim is the contract. |
| Assemblies | Hierarchy, transforms, BOM | Stable | Promoted 2026-08-21: deep-hierarchy transform composition verified against direct matrices, rotation+translation bounding boxes against transformed corners, sub-assembly nodes' own solids counted by flatten and BOM alike, deterministic ordered storage (BTreeMap) with BOM sorted by solid index, empty-assembly bbox a typed error (`qualify_assembly.rs`). |
| Evolution | Face provenance (booleans, blends, patterns, draft, defeature, split, shell) | Stable (declared coverage) | Retained; scope widened, label unchanged. Exact construction-derived provenance covers booleans (GFA face origins), walking and planar fillet/chamfer builders, and patterns. The planar builders carry each face specification through assembly; rolling fillets also carry it through production same-surface unification. The versioned WASM blend payload enumerates complete source/result handle domains, makes deletions and uncertainty explicit, and rejects duplicate, contradictory, phantom, or incomplete claims. Stable fillet/chamfer claims are never inferred from proximity, traversal order, or approximate geometry. The older scale-relative geometric matcher remains available to other Rust evolution routes and marks ambiguous output `unresolved`. Promoted 2026-08-21: draft, defeature, split, and shell now record construction-derived face evolution (`qualify_evolution_coverage.rs` pins total attribution — every result face claimed exactly once) and journal real entries via `*_journaled` wrappers. Declared remainder: offset and direct edits journal as explicit barriers; fallback blend engines without construction records report explicit unavailable provenance; edge/vertex provenance beyond the boolean path remains the P1 roadmap item. |
| Defeaturing | Planar face removal | Stable (declared domain) | Promoted 2026-08-21: cap strategy (through-hole, blind pocket, boss, cylindrical bore wall) and extend strategy (chamfer removal) qualified with exact restored volumes across 1e-3/1/1e3 scale; typed refusals for cavity solids, curved kept faces, over-removal; deterministic; WASM batch covered (`qualify_defeature.rs`). |

Issue 5.4 adds one bounded in-review modifier cell without broadening the
overall label: optional `startSetback`/`endSetback` distances on straight
spines may close a planar 3+-way variable S-curve junction when every active
endpoint has one common stationary radius and tangent ball. Its exact sphere
cap, G1 seams, physical result stations, closed/manifold B-Rep, watertight
mesh, independent volume agreement, exact census row, direct/batch WASM
parity, and transactional mismatch refusal are pinned. Other setback and
general mixed-radius corner topologies remain Experimental and fail closed.

The evidence required to lift any blocked stable row is the full gate set in
the audit request: documented domain/error/fallback behavior, negative and
boundary regressions, bounded iteration, validated watertight output, native
and WASM consistency, determinism, CI coverage, and a representative
integration result.
