{
  "private": true,
  "name": "remus-workspace",
  "description": "Development tooling for the Remus monorepo",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^21.0.2",
    "husky": "^9.0.0",
    "prettier": "^3.9.6"
  }
}
